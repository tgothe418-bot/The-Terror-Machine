## HG2 Repair Packet — plan

**Context:** HG2 Series 1 sits uncommitted in the working tree (~1,100 insertions, tests 1,495 green). My review found: (1) a phantom cadence — `pacingGovernor.ts` transitions to and switch-cases `'MOUNTING_PRESSURE'`, a value absent from `PacingCadenceSchema` (real value: `MOUNTING_COMPLICATION`), emitted live per the benchmark report; (2) `DramaturgyRuntimeStateSchema` exists but is never enforced at any live boundary (hydration trusts persisted state; governor output enters post-state unvalidated); (3) +65 tsc / +14 lint new errors; (4) benchmark report self-contradictions. Decisions locked with you: clear **new debt only** (inherited 85/193 stays for a future packet), **re-run the battery** after fixes.

### Packet R0 — Snapshot commit (local only, NO push)
Commit the existing WIP as-is first, so repair diffs are separately reviewable:
`feat(engine): HG2 Packet Series 1 — pacing governor, dramatic spine, clocks & composure (pre-repair snapshot)`

### Packet R1 — Phantom cadence + contract enforcement
1. **Fix:** rename 5× `MOUNTING_PRESSURE` → `MOUNTING_COMPLICATION` in `src/lib/pacingGovernor.ts` (lines ~83, 96, 147, 151, 156). One rename heals both defects: the state machine transitions to a valid enum value, and the mandate-text switch becomes exhaustive (previously a legitimate `MOUNTING_COMPLICATION` state rendered an `undefined` mandate).
2. **Tests:** update the 3 phantom references in `pacingGovernor.test.ts` (lines 32, 42, 210); add an exhaustiveness test — mandate text is non-empty for all 4 cadences × 3 seat branches (survivor/villain/director).
3. **Enforce at hydration:** in `buildEngineTurnContext.ts` (~line 641), `safeParse` persisted dramaturgy state with `DramaturgyRuntimeStateSchema`; on failure repair to `initializeDramaturgyRuntimeState` defaults + console forensic note. (Dramaturgy state is engine-derived and re-derivable; repair keeps sessions playable while making corruption visible — matches the project's normalization philosophy.)
4. **Enforce at output:** in `server/routes/turn.ts` after `executePacingGovernor`, `safeParse` `nextRuntimeState`; on failure **fail the turn closed** (engine-computed state corruption is fail-closed per the Laws of the House).
5. **New tests:** hydration with invalid persisted cadence → repaired, no throw; state-machine sweep over all 4 cadences × consecutive turns 0–3 → output always schema-valid.

### Packet R2 — Clear new gate debt (new only)
1. Enumerate current tsc/lint errors, filter to the 24 WIP-touched/new files, fix those. Expected cluster roots: loose sidecar typing (partially cured by R1's enforcement), fall-through switch, unused vars in the new UI code.
2. Gate check: tsc ≤ 85, lint ≤ 193 (baseline restored), full Vitest green, clean build.

### Packet R3 — Honest evidence
1. **Prerequisite:** LM Studio running with `gemma-4-26b-a4b-qat` loaded. If unavailable, I stop and report — no simulated results.
2. Re-run `scripts/run_hg2_40turn_benchmark.ts` (~15–20 min); verify the regenerated report: only valid cadence labels, summary table consistent with detail sections, D3 counts matching narrative, phase transitions milestone-gated.
3. Commit repairs (local only). **I will not push** — push remains your trigger.

### Standing constraints
No scenario character names in engine code; the model proposes, the machine decides; failed validation preserves canonical state with a receipt; all commits local, none pushed.

**Order:** R0 → R1 → R2 → R3, focused proofs per packet, broad gates at R2 and the end.